spring:
  # ... (기존 설정 유지)
  datasource: # 이 부분을 추가해야 합니다.
    url: jdbc:h2:mem:testdb # 고정된 이름을 사용 (원하는 이름으로 설정 가능)
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console # path 설정도 명시적으로 해주면 좋습니다.
  security:
    user:
      name: user
      password: mypassword
jwt:
  issuer: ajufresh@gmail.com